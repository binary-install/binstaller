{
  "name": "binstaller-schema",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "compile": "tsp compile .",
    "watch": "tsp compile . --watch",
    "gen:schema": "npm run compile",
    "gen:go": "./gen-go-with-fork.sh"
  },
  "devDependencies": {
    "@typespec/compiler": "latest",
    "@typespec/json-schema": "latest",
    "quicktype": "latest"
  }
}